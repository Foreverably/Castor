/**
 * Emoji enumerations. These are emojis put into the main discord bot, not the development one.
 * Yes the development bot will show a malformed expression.
 * No I don't care.
 *
 * and yes this is overly engineered when it can just be hardcoded in a string :)
 */
export enum CustomEmoji
{
    loading = "<a:loading:1508597963662823444>",
    check = "<:check:1510023211889459210>",
}
